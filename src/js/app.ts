import * as styles from "../assets/css/style-ts/style.css";

const greetings: string = "Hello world!!!!";
console.log(`${greetings} ${1 + 1}`);
let element = `<div class="${styles.elementts}">
  <p>Text from Typescript (TS) file.</p>
  </div>`;

document.write(element);
